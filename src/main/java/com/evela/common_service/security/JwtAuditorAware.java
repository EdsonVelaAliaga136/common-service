package com.evela.common_service.security;

import org.springframework.stereotype.Component;

//@Component
public class JwtAuditorAware {

    /*private final JwtTokenUtil jwtTokenUtil;

    public JwtAuditorAware(JwtTokenUtil jwtTokenUtil) {
        this.jwtTokenUtil = jwtTokenUtil;
    }

    public String getCurrentAuditor(String token) {
        return jwtTokenUtil.getUsernameFromToken(token);  // Usamos el JWT para obtener el auditor
    }*/

}
